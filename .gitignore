# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/

# App bundle produced by build
Products/

# Installer build material — the author's working files, not part of the code.
# Anchored to the repository root on purpose: the app's real artwork lives in
# CWMac/Assets.xcassets and must stay tracked.
/*.pkgbuilder
/*.tiff
/CWMac */

# The installer wizard needs the licence as a .txt next to it, so a second copy
# of LICENSE shows up in the root. The tracked LICENSE file is the real one.
/LICENSE.txt
